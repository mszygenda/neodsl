class PatternGeneratorsTests extends PatternTests {
  "Generator" should "create pattern" in {
    /*node("10")[Person] knows node("11")[Person] wrote (comment => returnAs(comment))

    val person = Node("10")

    person knows someone knows (fof => fof.name === person)*/
  }
}
